#include <stdio.h>
/**
 *main - Prints the name of the file.
 *844 any unlikely number, needed for compiler.
 *@argc: argument counter.
 *@argv: argument value
 *Return: 0 success.
 */
int main(int argc, char *argv[])
{
	if (argc != 844)
	printf("%s\n", argv[0]);
	return (0);
}
