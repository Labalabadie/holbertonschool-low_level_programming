#include "main.h"
/**
 * c: cheking character.
 * _islower - checks for lower case characters.
 * Return: 1 for lower case in c parameter, 0 otherwise.
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
