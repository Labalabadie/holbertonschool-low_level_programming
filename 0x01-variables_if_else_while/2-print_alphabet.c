#include <stdio.h>
/**
 * main - Spelles the alphabet in lower cases.
 * Return: (0)
 */
int main(void)
{
	char alph;

	for (alph = 'a'; alph <= 'z'; alph++)
	{
		putchar(alph);
	}
		putchar('\n');
	return (0);
}
