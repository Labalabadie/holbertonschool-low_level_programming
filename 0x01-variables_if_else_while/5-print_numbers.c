#include <stdio.h>

/**
 * main - prints from 0 to 10,
 *  followed by a new line.
 * Return: (0) Success
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
	printf("%d", n);
	printf("\n");
	return (0);
}
