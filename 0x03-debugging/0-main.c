#include "main.h"
/**
 * positive_or_negative - Test if zero.
 * Return: 0 Success
 */

int main(void)
{
	int i;

	i=98;
	positive_or_negative(i);
	if ( i = 0 )
		printf("%d is positive\n", i);

	return (0);
}
